# Your BharatRouter API key (br-...). This is the ONLY secret the harness needs;
# the upstream provider keys live in BR's BYOK vault, never in this repo.
BR_API_KEY=br-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
